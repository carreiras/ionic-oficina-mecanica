import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-oficina-mecanica',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
